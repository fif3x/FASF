#include <vector>
#include "FSAF.hpp" //include all needed libraries
#include <string>

int main() {

	//To test the example code, you may replace a comment with actual logic, for example the comment with the vector "carBrands" and the comment saying "FSAF::vector::scan<std::string>(carBrands, "Fiat");"

	//std::vector<std::string> carBrands = {"Ford", "BMW", "Fiat", "Tesla", "Ferrari", "Renault", "Toyota"};

	//std::vector<int> integerArray{ 1, 3, 199, 781, 5151, 5643, 0 };

	//FASF::vector::scan<std::string>(carBrands, "Fiat");

	//FASF::vector::scan<int>(integerArray, 781);

	return 0;
}
